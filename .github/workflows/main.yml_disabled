# name: deploy

# on:
#   push:
#     branches:
#       - master
#     paths-ignore:
#       - "**/terraform/**"
#       - "**/README.md"
#       - "**/.gitignore"

# jobs:
#   copy_files:
#     runs-on: ubuntu-slim
#     steps:
#       - uses: actions/checkout@v6
#       - name: rsync deployments
#         uses: burnett01/rsync-deployments@v8
#         with:
#           switches: -avzr --quiet --include="pritunl.yaml" --exclude="*"
#           remote_path: /home/opc
#           remote_host: ${{ secrets.DEPLOY_HOST }}
#           remote_port: ${{ secrets.DEPLOY_PORT }}
#           remote_user: ${{ secrets.DEPLOY_USER }}
#           remote_key: ${{ secrets.DEPLOY_KEY }}

#   start_docker:
#     needs: copy_files
#     runs-on: ubuntu-slim
#     steps:
#       - name: create secrets
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.DEPLOY_HOST }}
#           username: ${{ secrets.DEPLOY_USER }}
#           key: ${{ secrets.DEPLOY_KEY }}
#           port: ${{ secrets.DEPLOY_PORT }}
#           script: cd /home/opc; echo "${{ secrets.DOCKER_COMPOSE_ENV }}" > .env
#       - name: update images
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.DEPLOY_HOST }}
#           username: ${{ secrets.DEPLOY_USER }}
#           key: ${{ secrets.DEPLOY_KEY }}
#           port: ${{ secrets.DEPLOY_PORT }}
#           script: cd /home/opc; docker compose pull
#       - name: start services
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.DEPLOY_HOST }}
#           username: ${{ secrets.DEPLOY_USER }}
#           key: ${{ secrets.DEPLOY_KEY }}
#           port: ${{ secrets.DEPLOY_PORT }}
#           script: cd /home/opc; docker compose up -d --remove-orphans
